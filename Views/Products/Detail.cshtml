@model piano_store.Models.Product

<!DOCTYPE html>

<html>
<head>
	<title>Southview Piano</title>
</head>
<body>

	<img src="@Model.ImageUrl"/>
	<h3>@Model.Name</h3>
	<p>@Model.Detail</p>
	<h5>@Model.Price.ToString("c")</h5>

</body>
</html>